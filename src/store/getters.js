const getters = {
  language: state => state.view.language,
  resizeValue: state => state.view.resizeValue,
  componentLibraryVisible: state => state.view.componentLibraryVisible
}
export default getters
